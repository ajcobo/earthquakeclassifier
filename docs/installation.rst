============
Installation
============

At the command line::

    $ easy_install earthquakeclassifier

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv earthquakeclassifier
    $ pip install earthquakeclassifier
